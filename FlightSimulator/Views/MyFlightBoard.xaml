<UserControl x:Class="FlightSimulator.Views.MyFlightBoard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightSimulator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal" Margin="10">
            <Button x:Name="Connect"
                Content="Connect"
                Click="ClickConnect"
                ClickMode="Press"  
                Margin="0,0,10,0"
                    Height="20"
                    Width="75"
                HorizontalAlignment="Center"
                TextElement.FontWeight="Bold"
                Foreground="Gray"/>
            
            <Button x:Name="Settings"
                    Content="Settings"
                    Click="ClickSettings"
                    ClickMode="Press"
                    Height="20"
                    TextElement.FontWeight="Bold"
                    Width="75"
                    HorizontalAlignment="Center"
                    Foreground="Gray"/>
        </StackPanel>
        <local:FlightBoard HorizontalAlignment="Center" Grid.ColumnSpan="2" Grid.Row="1" Loaded="FlightBoard_Loaded"/>
    </Grid>
</UserControl>
